name: Rust

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

env:
  CARGO_TERM_COLOR: always

jobs:
  build-dev:

    runs-on: ubuntu-latest

    if: github.ref_name == 'dev'
    environment: build-and-test-for-dev
    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: |
        echo "DPTRAN_DEEPL_API_TOKEN=${{ vars.DPTRAN_DEEPL_API_TOKEN }}" >> "$GITHUB_ENV"
        cargo test --verbose

  build-main:

    runs-on: ubuntu-latest
    
    if: github.ref_name == 'main'
    environment: build-and-test
    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: |
        echo "DPTRAN_DEEPL_API_TOKEN=${{ vars.DPTRAN_DEEPL_API_TOKEN }}" >> "$GITHUB_ENV"
        cargo test --verbose
